# Binggo

本机（local-first）B 站抽奖助手的领域语言：从数据源发现抽奖活动，落入本机活动库，
经 Web 控制台或定时调度参与，再做中奖提醒与清理。本文件只定义词，不记录约束——
不变量与其守卫测试见 `SPEC.md` 第 8 节。

## Language

### 活动与抽奖

**活动**：
一条被判定为抽奖的 B 站动态，以 `dynamic_id` 唯一标识。
_Avoid_: 抽奖帖、动态（未加限定时）

**抽奖类型**：
决定参与动作集的活动分类，可参与的三种为互动抽奖、转发抽奖、预约抽奖。
_Avoid_: 玩法、活动类型

**预约抽奖**：
只需关注 + 预约两个动作的抽奖类型，不走五连。
_Avoid_: 预约、reserve

**活动链接**：
数据源从容器正文中提取出的、指向单条活动的链接，是流水线的输入。
_Avoid_: 链接、URL

### 参与

**参与**：
对单个活动执行其类型所需的全部动作，并写入该账号的参与记录。
_Avoid_: 打卡、执行

**五连**：
单个活动内执行的五个动作：点赞、关注、收藏、转发、评论。数的是**动作**，
不是活动个数（对照三连）。关注是临时关注分区操作的前置，其余四个互不依赖、顺序可换。
_Avoid_: 三连（不同轴）、一键三连

**三连**：
一个参与任务中并发处理的**活动个数**（上限 3）。数的是**活动**，
不是动作个数（对照五连）。与 B 站文案里的「三连」无关。
_Avoid_: 五连（不同轴）

**三连（平台语义）**：
UP 主在活动正文中要求的点赞 + 收藏 + 投币，是被解析的**外部**词汇，
不是 Binggo 的行为。仅出现在 `forward_parser` 的准入判定中。
_Avoid_: 与上一条混用

**参与状态**：
某账号对某活动的参与结果，按 `(uid, dynamic_id)` 存储，是账号参与与否的**唯一权威**。
_Avoid_: 活动状态、状态

### 状态与事实

**共享展示状态**：
活动行上跨账号共用的展示字段（`activity_status`）。它**不是**权威——
可参与活动的展示状态一律由「已结束 > 参与状态 > 平台事实 > 默认」重新判定。
_Avoid_: 把它当作账号参与状态的依据

**开奖状态**：
活动本身是否已开奖（进行中 / 已结束），由开奖时间决定，与账号无关。
_Avoid_: 活动状态

**开奖提示**：
基于开奖时间派生的时效标签，当前只有「即将开奖」一个取值。
_Avoid_: 开奖状态

**平台事实**：
从带登录态接口读到的平台侧结论（是否已参与、是否已预约）及其观测身份，
三者是不可分割的元组，必须同进同出。
_Avoid_: 单独引用其中一个字段

### 身份

**生效身份**：
当前实际发出 B 站请求的账号，按「环境变量 > 活跃账号 > cookies.txt」解析。
UI 显示与实际请求身份必须是同一个。
_Avoid_: 当前账号、用户

**活跃账号**：
账号池中被标记为 active 的账号，是生效身份的候选之一，不等于生效身份。
_Avoid_: 当前账号

**绑定身份**：
任务创建时由服务端写定的执行账号，用于准入检查与审计，不是凭据冻结。
_Avoid_: 当前账号

**观测身份**：
产生某条平台事实时**实际发出请求**的账号，不是"当时的活跃账号"。
_Avoid_: 当前账号、活跃账号

**账号上下文**：
任务启动时捕获的不可变凭据快照（Cookie、CSRF、UID、代理），整个任务复用同一份。
_Avoid_: 会话、登录态

**环境身份**：
未提供账号上下文时，`BilibiliClient` 按生效身份就地解析出的账号。它随活跃账号变化，
不冻结。与账号上下文是**互斥的两种身份模式**，构造点上看不出用的是哪一种。
_Avoid_: 默认身份、当前账号

**刻度**：
调度器的一个时间触发点。三类：刷新批次（整点 `:00`）、中奖深检（整点 `:30`）、
三连参与（非整点小时的 `:05`~`:55`）。
_Avoid_: 时间片、tick、任务槽

**槽 key**：
刻度的字符串标识（`2026-09-10-14-05`），既用于去重「这个刻度跑过没有」，
也是轮转账号的**唯一输入**。同一个槽 key 恒定映射到同一账号。
_Avoid_: 时间戳、slot id

**轮转**：
把连续的刻度依次分配给账号池中不同账号的串行机制。轮到谁从槽 key 派生，
**不存游标**，因此它不记得上一轮发生过什么。它承诺每个号都会轮到，不承诺公平配额。
_Avoid_: 负载均衡、调度策略、账号池化

### 数据源

**数据源**：
活动链接的来源，编号 DS-1~10，涵盖 UP 合集、手动清单、话题与外部 API。
_Avoid_: 源、渠道

**容器**：
数据源上承载多条活动链接的那一篇内容，形态随源而异（视频、专栏、Opus 帖）。
_Avoid_: 帖子、文章

**指纹**：
容器内容的摘要值（sha256 / ETag / Last-Modified），用于判断内容是否变化。
_Avoid_: 版本号、hash

**checkpoint**：
每个数据源上次检查的位置记录，指纹即存放于此。
_Avoid_: 游标、进度

### 机制

（以下词条只定义"是什么"；取值枚举与约束见 `SPEC.md` §8）

**任务**：
一次有副作用的后台执行单元，同一时刻本机只允许一个任务级写者。
_Avoid_: 作业、job

**写者锁**：
保证同一时刻只有一个任务级写者的跨进程文件锁。它**不**覆盖 Web 读路径的
维护性写入与配置类改动。见 SPEC §8 不变量 #7。
_Avoid_: 全局锁、数据库锁

**任务身份策略**：
每个任务动作必须显式登记的身份要求（unbound / bound / context），未登记者拒绝启动。
见 SPEC §8 不变量 #5。
_Avoid_: 权限

**error_kind**：
任务终态的结构化分类，用于替代按中文 message 判定结果。见 SPEC §8 不变量 #11。
_Avoid_: 错误码、状态

**时间槽**：
定时调度器触发任务的固定时刻。撞车时跳过本槽，不停机。
_Avoid_: 定时器、cron

### 清理与中奖

**清理**：
回收 Binggo 自己创建的转发与关注，默认预演。
_Avoid_: 删除、回收

**归属证据**：
证明某条动态或某个关注确由 Binggo 创建的精确记录，是清理的唯一依据。
见 SPEC §8 不变量 #10。
_Avoid_: 标记、来源

**白名单**：
同时豁免删除与取关的保护名单。
_Avoid_: 例外、忽略列表

**临时关注分区**：
参与时新建的关注被移入的 B 站关注分区，供清理回收。
_Avoid_: 分组、文件夹

**开奖时间**：
活动公布结果的时刻，是判定已结束与即将开奖的唯一依据。
_Avoid_: 截止时间、deadline

**中奖深检**：
逐条核对私信与通知以确认是否中奖的检查。
_Avoid_: 检查、轮询

**送达确认**：
通知至少经一个渠道确认到达；未确认前不得将私信标记为已读。
_Avoid_: 发送成功
